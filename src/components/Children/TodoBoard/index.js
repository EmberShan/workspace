// use react-dnd to create to do boards that 
// can also drag the todo items across different boards 
import React from 'react'

function TodoBoard() {

    return (
        <div>

        </div>
    )
}

export default TodoBoard
